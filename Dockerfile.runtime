# Runtime base image for manim-gpt
# Contains all runtime dependencies (Python, TeX Live, ffmpeg, graphics libraries)
# Push to Docker Hub: ajha2025/manim-gpt:runtime-*
# Rebuild when runtime dependencies change (rarely)

FROM debian:bookworm-slim AS manim-runtime-base

# Install ONLY runtime dependencies (no build tools or -dev packages)
RUN apt-get update && apt-get install -y \
    # Shell and process management
    bash \
    procps \
    # Python runtime (Debian Bookworm has Python 3.11)
    python3 \
    python3-pip \
    # Graphics runtime libraries (NO -dev packages)
    libcairo2 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libglib2.0-0 \
    libharfbuzz0b \
    libfontconfig1 \
    fontconfig \
    libfreetype6 \
    libpixman-1-0 \
    libpng16-16 \
    # X11 runtime libraries
    libxcb1 \
    libx11-6 \
    libxcb-render0 \
    libxcb-shm0 \
    # Media processing
    ffmpeg \
    # Document processing
    ghostscript \
    # TeX Live for LaTeX rendering
    texlive-latex-base \
    texlive-latex-extra \
    texlive-latex-recommended \
    texlive-fonts-recommended \
    texlive-fonts-extra \
    texlive-science \
    dvisvgm \
    # Additional font packages for proper rendering
    cm-super \
    lmodern \
    # Utilities
    curl \
    ca-certificates \
    # Fonts for Manim
    fonts-liberation \
    fonts-dejavu-core \
    fonts-noto \
    gsfonts \
    && rm -rf /var/lib/apt/lists/* \
    && apt-get clean

# Optimize TeX Live size by removing docs and sources (saves ~500MB)
RUN rm -rf /usr/share/texlive/texmf-dist/doc && \
    rm -rf /usr/share/texlive/texmf-dist/source

# Update font cache to ensure all fonts are indexed
RUN fc-cache -fv

# Set environment variables
ENV PYTHONUNBUFFERED=1 \
    LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu

WORKDIR /app

# Image metadata
LABEL maintainer="Ashish Jha"
LABEL description="Runtime base image for manim-gpt with all runtime dependencies"
LABEL version="1.0.0"
